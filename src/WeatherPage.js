import React from "react";

export default function WeatherPage() {
  return (
    <div
      style={{
        background: "#fff",
        padding: "20px",
        borderRadius: "20px",
      }}
    >
      Weather Page ✅
    </div>
  );
}
